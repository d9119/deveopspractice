name: Greeting Workflow

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run a greeting
        run: echo "The CI pipeline has successfully started!"

      - name: Install pytest
        run: pip install pytest

      - name: Run Python Tests
        run: pytest

